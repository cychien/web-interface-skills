---
name: web-interface
description: Design engineering principles that separate a working web UI from a crafted one. Use whenever building a web interface - any component, page, or layout.
---

# Web Interface

Read `principles/<name>.md` only for the rows whose trigger matches what you are building. Do not read them all. When reviewing finished UI, walk the checklists of the principles that apply and report only genuine violations.

| Principle | Read it when |
| --- | --- |
| `safe-area` | **Always.** Every page shell needs the baseline; edge-to-edge layouts, fixed headers, bottom bars, composers, PWAs and WebViews need the rest of it |
