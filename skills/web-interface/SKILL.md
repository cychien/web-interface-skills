---
name: web-interface
description: Design engineering principles that separate a working web UI from a crafted one. Use whenever building a web interface - any component, page, or layout.
---

# Web Interface

## How to use this skill

1. Read **Always** below. It is short, universal, and applies to every UI change.
2. Scan the **Principles** index. Read `principles/<name>.md` only for the rows whose trigger matches what you are actually building.
3. When reviewing UI, walk the index as a checklist and report only the rows that are genuinely violated.

Do not read every principle file. The index exists so that you do not have to.

## Always

Universal one-line rules live here, inline, so they cost nothing extra to apply.

_Nothing yet._

## Principles

Each row maps to `principles/<Principle>.md`.

| Principle | Read it when |
| --- | --- |
| `safe-area` | Important UI can reach a viewport edge - fixed or sticky headers, bottom bars, composers, edge-to-edge or full-screen layouts, PWAs and WebViews |

Groups to file new entries under once the table needs headings: **layout and space**, **shape and depth**, **type and rhythm**, **states and interaction**, **motion**, **responsiveness and device**, **content and copy**. Add a group when three entries want one, not before.
